job_name: test_basic_extract_load

steps:
  - name: load_products
    source:
      connection: source_oracle
      query: "SELECT product_id, name, description, price, category, created_at FROM products"
    target:
      connection: target_oracle
      table: stg_products
      batch_size: 5000
